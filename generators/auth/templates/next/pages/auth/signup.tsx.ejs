import { AuthForm } from "@lib/auth/ui/AuthForm";

const Page = () => {
  return (
    <main className="container">
      <hgroup className="center">
        <h1>Sign Up</h1>
        <h2>Authentication generated with FX</h2>
      </hgroup>
      <AuthForm authType="signup" submitButtonText="Sign up" />
      <% if (props.f.hasScopeGoogle({ props })) { -%>
      <a role="button" className="secondary full" href="/api/auth/google">
        Google
      </a>
      <% } -%>
    </main>
  );
};


// Some basic styles, remove this block after generation.
Page.getLayout = (page: React.ReactElement) => {
  return (
    <>
      <link
        rel="stylesheet"
        href="https://unpkg.com/breezycss@latest/css/breezy.min.css"
      />
      <style global jsx>{`
        .container {
          padding: 6rem 1rem;
          max-width: 360px;
          margin: 0 auto;
        }

        .center {
          text-align: center;
        }
      `}</style>
      {page}
    </>
  );
};

export default Page;
