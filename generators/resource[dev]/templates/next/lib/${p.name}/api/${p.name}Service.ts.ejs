import { prisma } from "@api/prisma";
import { <%= p.pascalName %>, Prisma } from "@prisma/client";

export const create<%= p.pascalName %> = async (input: Prisma.<%= p.pascalName %>CreateInput) => {
  return prisma.<%= p.camelName %>.create({
    data: input,
  });
};

export const get<%= p.pascalName %> = async ({ id }: Pick<<%= p.pascalName %>, "id">) => {
  return prisma.<%= p.camelName %>.findUnique({
    where: { id },
  });
};

export const get<%= p.pascalName %>List = async () => {
  return prisma.<%= p.camelName %>.findMany();
};

export const update<%= p.pascalName %> = async (
  input: Pick<<%= p.pascalName %>, "id"> & Prisma.<%= p.pascalName %>UpdateInput
) => {
  const { id, ...data } = input;
  return prisma.<%= p.camelName %>.update({
    where: { id },
    data,
  });
};

export const delete<%= props.pascalName %> = async ({ id }: Pick<<%= props.pascalName %>, "id">) => {
  return prisma.<%= p.camelName %>.delete({
    where: { id },
  });
};
